# CHANGELOG

All notable changes. Conventional-commit driven.

## [Unreleased]

### Added
- 2026-08-25 feedback round 7 (boost, powerups, collision rules, pace): BOOST fixed — drain is now proportional (1.5%/s of current mass, floor 0.8/s) and the gate sits a hair above spawn mass, so players boost continuously all the way down to spawn size (previously flat 3 mass/s + a gate at 12 killed a small worm's boost in ~2.7s). POWERUPS: every one lasts exactly 10s; new 5X and 10X score multipliers (hand-vectored ×5 gold / ×10 red badges beside the ×2 purple; orbiting star count/color scales with the multiplier; highest active wins); world powerup density raised 5× (30 per 25 players, cap 90) so powerup foods are a normal sight. COLLISIONS: head-on crashes now kill only the SMALLER worm (within 5% mass: both) — contact within two head-radii counts as head-on; and the giant ghost-tail bug is dead: maxPathSamples 2048→4096 covers ~40k wu of body and the renderer clamps drawn length to collidable coverage, so a rendered tail is always a killing tail (regression: rear-ending a mass-6000 giant's tail tip). PACE: base speed 180→240 wu/s with turn rates scaled in proportion (5.9/2.9), preserving every coil/turn-radius rule. Physics test suite now 5 tests incl. head-on winner, equal-mass tie, giant-tail kill. Bonus: assets-manifest.json no longer embeds raw image bytes — 29MB → 6KB on every page load.
- 2026-08-25 feedback round 6 (minimap leader + intelligent bots): The current #1 player is now marked on the minimap with a red crowned dot (white ring) so everyone can see where the leader is. Bot AI upgraded to human-like play: bots race to DEATH_LOOT corpse drops first (1400wu scan, boosting to beat rivals to the kill site), hunt actively with lead-intercept cut-offs (aiming where the prey's head is GOING so the bot's body lands across its path — the classic slither kill move, with overtake boosts), and probe ~0.6s ahead of their own head for any worm body in their path, steering around it like a human who can see — eliminating blind body crashes. Verified live: bot scores climbing steadily (leader 30→159 in 40s).
- 2026-08-25 feedback round 5 (body cohesion + squeeze pacing + bot lifetime): Body segment spacing now scales with the worm's RADIUS instead of length/segment-cap, so a giant worm's circles stay snugly overlapped exactly like at spawn (previously a mass-6500 worm showed 270wu gaps between 70wu circles); segment cap raised to 900 as a perf-only backstop. Encircle squeeze re-paced to wormate feel: turn floor 2.7→2.2 rad/s so the coil closes over smooth laps (never a dive), trapped AI bots now DETECT enclosure (8-sector body-bearing scan) and gracefully circle the shrinking space hugging its center — surviving until the ring is genuinely smaller than their turning circle — and the regression test now proves the full wormate sequence: smooth shrink, hold outside prey's reach, decisive close, prey dies at ~12s (never <2s) with the encircler unharmed. Bot lifetime rule per user: NO artificial rotation — bots live until actually killed; fallen bots are replaced by fresh random identities to hold the arena at 50.
- 2026-08-24 feedback round 4 (bot lifecycle + encircle physics): Bot population reworked — bots trickle in one at a time (2.5s stagger, never simultaneously) up to 50 per arena; once full, every 30s the longest-lived bot dies (dropping loot) and a fresh one joins, rotating 1→50 forever; naturally-killed bots rejoin as brand-new identities. Bot names are now procedurally random every spawn (word+word / syllable mash / word+number — e.g. IronWolf806, Grofuren, SilentPanda) instead of a fixed roster. Encirclement physics fixed to wormate parity: big-worm turn floor raised 1.6→2.7 rad/s (mass ramp 500→1000) so a coiling worm's tightest ring (~67 wu) squeezes below a trapped worm's escape circle — encircled prey slowly runs out of room and dies against the surrounding body (previously the coil bottomed out at ~112 wu and trapped worms could orbit inside forever); locked by 2 new game-core regression tests (32 total). Global top-10 leaderboard moved out of the play flow to a right-side panel (falls back into the column below PLAY under 900px); removed an accidental duplicated CSS block.
- 2026-08-24 feedback round 3 (social + world feel): FIXED the stuck tail when coiling — client body rendering now samples the head's actual arc-length path (PathTracker) exactly like the authoritative sim, replacing chain-follow segments that froze when compressed. Resident smart AI worms (personality-driven: wall avoidance, food seeking, threat/prey reactions, boost bursts) always populate bot-enabled arenas, play through the same input pipeline as humans, appear on the live leaderboard/rank, die/drop loot/respawn, and yield seats as humans join (floor 8 players, cap 8 bots). World grown 12000→18000 wu with ambient food scaled to match and fast initial fill. Wormate-style square minimap (bounds + own gold dot + nearby player dots, 5 Hz canvas). Room invite links: 📋 button copies ?join=<roomId> URL, friends land in the exact same room (E2E-verified). Global top-10 leaderboard (best scores from Postgres, 30s cache, humans only) on the home screen. +4 E2E tests (bots ranked, channel isolation, invite same-room, minimap) → 23 total, all green.
- 2026-08-24 feedback round 2: FIXED the persistent movement bump — root cause was the M3 input rate limiter (client 60 inputs/s vs 40/s cap ⇒ steering blackout at every window tail; reproduced 108/308 rejected, now 0). Premium art v2: banded ring worm bodies (tintable grayscale orb, chunky radius-scaled bands), expressive heads (big forward eyes, brows, open smile), 24-variant dessert food set with 2.5D extrusion + baked glow (drops/cookie/candy-heart/macarons/bonbon/gummy bears/lollipops/donuts/cake slices), radiant pulsing death-loot with additive glow, boost glow, floating +score popups, food ~50% larger on screen. Cache-correct asset URLs (?v=content-hash from manifest) fix stale-atlas misaligned frames ("half foods").
- 2026-08-23 polish (user feedback): fixed-timestep render interpolation kills the ~1s movement judder (E2E-enforced smoothness metric); camera view-area normalization makes browser zoom/resize reveal nothing (E2E-enforced) + ctrl-zoom/pinch guards; wormate-style food art (6 candy drops, 3 bonbons, 2 lollipops, 2 donuts, soul orb — variant per pellet, bigger on screen); round badge powerup pickups; on-worm effect animations (shield bubble, magnet rings, speed streaks, growth sparkles, ×2 star orbit, ember trail); new ZOOM powerup with eased wide-view effect.
- 2026-08-23 M4: production packaging — PWA (manifest + static-shell service worker), legal/how-to-play pages + footer, client error telemetry endpoint (rate-limited), compose log rotation, staging overlay, backup/restore/healthwatch scripts, dependency-free WS smoke test, full runbook set (DEPLOYMENT/VPS/DOCKER/MONITORING/BACKUPS/OPERATIONS/RELEASES/TROUBLESHOOTING/SECURITY/PERFORMANCE/API/PRODUCT).
- 2026-08-23 M3: hardening — InputGuard anti-cheat (8 tests), food AOI via StateView (~76% state reduction), AI bots (seek/avoid/hunt/flee), load sweep 10→200 bots with recorded metrics (0 errors, tick avg <3ms), /metrics endpoint + client debug panel + network quality dot, ping/jitter measurement, PostgreSQL migrations + guest identity + async batched stats (verified vs PG16), Redis-backed rate limiting with in-memory fallback. Docker stack deployed + smoke-tested on user's machine.
- 2026-08-23 M2: product feel — powerups live end-to-end (SPEED/MAGNET/DOUBLE_GROWTH/SHIELD/BOOST_REDUCTION/SCORE_MULTIPLIER incl. magnet pull + shield block), full original art via procedural SVG pipeline (6 skins, 5 foods, 6 badges, wordmark/icon/favicon/social, bg tile) packed into an atlas, synthesized SFX + music with AudioManager, skin picker, settings modal (volumes/quality/reduced-motion), mobile joystick + boost button with safe areas, reconnect grace (server hold + client resume overlay), matchmaking channels. 30 game-core tests, 13 Playwright E2E.
- 2026-08-23 M1: playable multiplayer vertical slice — full deterministic simulation (food/growth, boost drops, collision → death → loot, spawn safety), client prediction + reconciliation, remote snapshot interpolation, DOM HUD (score/leaderboard/death screen), fakeLag network simulation, 45 unit tests + 9 Playwright E2E (two-client multiplayer, 150ms latency, death/respawn).
- 2026-08-23: repository bootstrap — monorepo skeleton, project rules (CLAUDE.md), master spec, docs structure.
